# agent-os — Python dependencies (aggregated from imports in agents/, mcp/, dashboard/).
# Requires Python 3.12. Use `make bootstrap-runtime`; the tested transitive
# dependency set lives in requirements.lock.txt.

# ── Core / DB ────────────────────────────────────────────────
psycopg2-binary
python-dotenv>=1.2.2

# ── LLM / agents ─────────────────────────────────────────────
groq>=1.6.0
praisonaiagents[llm]>=1.6.166,<1.7
langfuse
tiktoken

# ── MCP server ───────────────────────────────────────────────
mcp[cli]>=1.29.0,<2

# ── Vector memory / embeddings ───────────────────────────────
qdrant-client
sentence-transformers

# ── Messaging / integrations ─────────────────────────────────
python-telegram-bot
telethon
requests>=2.34.2
openpyxl
pypdf>=6.16.1,<7
python-docx>=1.2.0,<2
google-api-python-client
google-auth
google-auth-oauthlib

# ── Tests ────────────────────────────────────────────────────
pytest>=9.1.1
pip-audit>=2.10.1
bandit>=1.9.4
